"""Analyst agent: tool registry, Claude tool-use loop, offline playbook analyst, chat sessions.

Import the submodules directly (``throughline.agent.tools`` etc.); this package init stays import-light so the
API module loads even when optional dependencies are missing.
"""
